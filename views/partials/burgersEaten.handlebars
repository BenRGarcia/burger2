<div class="list-bg">
  <h2>
    3) Burgers You Have Eaten
  </h2>
  <ul class="text-left">
    {{#each burgers}}
      {{#if is_devoured}}
        <li>
          {{burger_name}} 
          eaten by 
          {{customer_name}}
        </li>
      {{/if}}
    {{/each}}
  </ul>
</div>